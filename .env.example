# Backend configuration
GEMINI_API_KEY=put-your-gemini-api-key-here
GEMINI_MODEL=gemini-2.5-flash
PORT=8090

# Frontend configuration
# Set this to the public WebSocket endpoint when deploying the backend.
# Leave empty for local development (the app will default to ws://localhost:8090).
VITE_WS_ENDPOINT=
